{
  "name": "mepply",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint '**/*.js'",
    "format": "prettier --write '**/*.js'",
    "test": "npm-run-all --parallel test:*",
    "test:auth-service": "cd auth-service && npm test",
    "test:scraping-matching-service": "cd scraping-matching-service && npm test"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1"
  }
}
